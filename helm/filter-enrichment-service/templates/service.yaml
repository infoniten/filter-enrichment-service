apiVersion: v1
kind: Service
metadata:
  name: {{ include "fes.name" . }}
  labels:
    {{- include "fes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fes.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
