package com.example.filterenrichment.domain;

/** Enrichment completeness reported downstream in the output {@code metadata} block (§21/§22/§28). */
public enum EnrichmentStatus {
    FULL,
    PARTIAL
}
